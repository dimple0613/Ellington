DATABASE_URL=postgresql://postgres@localhost:5432/developer_inventory
JWT_SECRET=change-me-in-production

# Initial admin credentials — used ONLY on first install/seed; auth always
# reads the database afterwards (Profile page updates the live credentials).
INITIAL_ADMIN_EMAIL=admin@gmail.com
INITIAL_ADMIN_PASSWORD=Admin123
